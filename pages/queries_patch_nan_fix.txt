
Open your `queries.py` and harden input normalization in `upsert_coin_type`:

def upsert_coin_type(master_id: int, year: int, mint_mark: str = None, variety: str = None, ...):
    NAN_LIKE = {'-','—','none','nan','NaN'}
    mint_mark = (mint_mark or '').strip()
    if mint_mark.lower() in NAN_LIKE: mint_mark = ''
    variety = (variety or '').strip()
    if variety.lower() in NAN_LIKE: variety = ''
    ...
